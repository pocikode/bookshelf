{
  "name": "readest-personal",
  "private": true,
  "license": "AGPL-3.0-only",
  "packageManager": "bun@1.4.2",
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@sindresorhus/tsconfig": "^6.0.0",
    "typescript": "^5"
  },
  "overrides": {
    "google-auth-library": "10.6.2",
    "marked-katex-extension": "5.1.10"
  },
  "scripts": {
    "prepare:frontend": "bash scripts/apply-readest-overrides.sh",
    "build:frontend": "bun run prepare:frontend && bun run --cwd .build/readest/apps/readest-app build-personal",
    "build:frontend:baseline": "bun run --cwd .build/readest/apps/readest-app build-web",
    "dev:backend": "go run ./server/cmd/readest",
    "start:backend": "go run ./server/cmd/readest",
    "typecheck": "bun run prepare:frontend && bun run --cwd .build/readest/apps/readest-app typecheck",
    "lint": "bun run prepare:frontend && bun run --cwd .build/readest/apps/readest-app lint",
    "test:frontend": "bun run prepare:frontend && bun run --cwd .build/readest/apps/readest-app test"
  }
}
