# Every variable is optional. Each is shown with the value the application uses
# when the variable is absent.

# Without APP_PASSWORD a random one is generated on first start, written to
# $DATA_DIR/.bootstrap-password (mode 0600), and logged once at startup. Setting
# it here takes over and invalidates existing sessions.
#APP_PASSWORD=replace-me-with-a-strong-password

# Container images default to /app/data. A plain binary defaults to the
# platform user data directory:
#   Linux/BSD  $XDG_DATA_HOME/bookshelf, else ~/.local/share/bookshelf
#   macOS      ~/Library/Application Support/bookshelf
#   Windows    %LOCALAPPDATA%\bookshelf
#DATA_DIR=

#PORT=8070
#MAX_UPLOAD_MB=400
#SESSION_DAYS=90
#TRUST_PROXY=false
#LOG_LEVEL=info
